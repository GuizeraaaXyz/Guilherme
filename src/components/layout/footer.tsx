import Link from "next/link";
import { Container } from "@/components/ui/container";
import { navLinks, services } from "@/data/content";
import { siteConfig, whatsappUrl } from "@/constants/site";
import { BrandLogo } from "@/components/shared/brand-logo";
export function Footer() { return <footer className="border-t border-white/8 bg-[#050810] py-14"><Container><div className="grid gap-10 md:grid-cols-2 lg:grid-cols-4"><div><Link href="/" aria-label={`${siteConfig.name}, início`}><BrandLogo className="w-[210px]" /></Link><p className="mt-4 max-w-xs text-sm leading-6 text-slate-400">{siteConfig.slogan}</p><p className="mt-5 text-sm text-slate-500">{siteConfig.location}</p></div><div><p className="font-semibold text-white">Navegação</p><div className="mt-4 grid gap-3">{navLinks.map(([l,h]) => <Link key={h} href={`/${h}`} className="text-sm text-slate-400 hover:text-white">{l}</Link>)}</div></div><div><p className="font-semibold text-white">Serviços</p><div className="mt-4 grid gap-3">{services.slice(0,4).map(s => <Link key={s.title} href="/#servicos" className="text-sm text-slate-400 hover:text-white">{s.title}</Link>)}</div></div><div><p className="font-semibold text-white">Contato</p><div className="mt-4 grid gap-3 text-sm"><Link href={whatsappUrl()} target="_blank" className="text-slate-400 hover:text-white">WhatsApp</Link><Link href={`mailto:${siteConfig.email}`} className="break-all text-slate-400 hover:text-white">{siteConfig.email}</Link>{siteConfig.instagram && <Link href={siteConfig.instagram}>Instagram</Link>}{siteConfig.github && <Link href={siteConfig.github}>GitHub</Link>}</div></div></div><div className="mt-12 flex flex-col gap-4 border-t border-white/8 pt-7 text-xs text-slate-500 sm:flex-row sm:items-center sm:justify-between"><p>© {new Date().getFullYear()} {siteConfig.name}. Todos os direitos reservados.</p><div className="flex gap-5"><Link href="/politica-de-privacidade" className="hover:text-white">Política de Privacidade</Link><Link href="/termos-de-uso" className="hover:text-white">Termos de Uso</Link></div></div></Container></footer>; }
